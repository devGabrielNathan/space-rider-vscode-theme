import { createTheme, type Theme } from "../index";

export const baseTheme: Theme = createTheme("Space Rider Base", "dark");
export const softTheme: Theme = createTheme("Space Rider Soft", "soft");
export const vividTheme: Theme = createTheme("Space Rider Vivid", "vivid");
